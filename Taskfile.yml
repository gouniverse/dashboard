# https://taskfile.dev

version: '3'

vars:
  GREETING: Welcome to Gouniverse Dashboard!

tasks:
  default:
    cmds:
      - echo "{{.GREETING}}"
    silent: true

  dev:
    cmds:
      - cd ./development; air